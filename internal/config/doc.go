// Package config manages saferm's TOML configuration and directory
// initialization. Configuration is loaded from ~/.saferm/config.toml
// with sensible defaults when the file is absent.
package config
